#pragma config(Sensor, S1,     HTSPB,                sensorI2CCustom9V)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

/*
HiTechnic Experimenter's Kit for the SuperPro
*/
//#define 	analogIn   0x42

/*
00
||-Clock pin
|
|--Data pin

*/
int distance;
void irSensor();

#include "hitechnic-superpro.h"

task main()
{
	while(true)
	{
		wait10Msec(10);
		eraseDisplay();
		irSensor();
	}
}
void irSensor()
{
	//distance = HTSPBreadADC(HTSPB,analog0,8);
  // distance = HTSPBreadADC(HTSPB, analog,8);
   distance = HTSPBreadADC(HTSPB, 0x42,8);
   writeDebugStream("int deistance is: %d", distance);
  //distance = HTSPBreadAllADC	(	HTSPB,int & 	adch0,int & 	adch1,int & 	adch2,int & 	adch3,int & 	adch4,byte 	width);	
   //nxtDisplayCenteredBigTextLine(4, "%d", distance);
	wait10Msec(10);
}
